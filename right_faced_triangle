def right_faced_triangle(num):
	pat = input("enter a pattern :  ")
	for i in range(1, num+1):
		y = (pat * i)
		print(y)
	for j in range(-num, 1):
		x = (pat * (-j))
		print(x)
	#print(x + y)
right_faced_triangle(int(input("enter the number  " )))
